# Beyond Majority Voting: Hint-guided SQL Selection with LLM
This work has been submitted to 1st round of ICDE 2027.

run the selection
```
python -u online.py --method_name alphasql --dataset_name {dataset} --model_name {model_name} --selector {selector}
```